def plan_weekend(prefs):
    print(f"Planning weekend based on preferences: {prefs}")
    return {"day": "Saturday", "type": "Art"}

# Optional alias (after defining the function!)
planner_agent = plan_weekend
